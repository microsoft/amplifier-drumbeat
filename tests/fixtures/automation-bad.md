---
automation:
  name: Bad Example
  enabled: false
  trigger:
    type: manual
  notify: never
---

1. This numbered list is the RETIRED body-steps shape. Steps are structured
   frontmatter data now (`steps:` in the `automation:` block), so this file is
   refused with a pointer to `contracts/automation-file.v1.md` rather than being
   silently ignored.
